package model.flags;

/**
 * This enum represents the flags of countries and it represents their names, The names are set
 * lexicographically.
 */
public enum EImageType {
  /**
   * FLAGS
   */
  FRANCE_FLAG,
  GREECE_FLAG,
  SWITZERLAND_FLAG,

  /**
   * OTHER
   */
  CHECKERBOARD
}
